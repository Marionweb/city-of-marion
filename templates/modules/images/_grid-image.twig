{%- import "macros/_images" as imageFunctions -%}
{% set outputWidths = [480, 640, 1024, 1600] %}
{%- set qual = 20 -%}
{%- if image.aspectRatio | length -%}
  {% set aspect = image.aspectRatio|split('x') %}
  {% set aspectRatio = (aspect[0]/aspect[1]) %}
  {%- set small = { width: 40, height: 40 / aspectRatio, quality: qual } -%}
  {%- set medium = { width: 100, height: 100 / aspectRatio, quality: qual } -%}
{%- else -%}
  {%- set small = { width: 40, quality: qual } -%}
  {# {%- set medium = { width: 640, quality: qual } -%} #}
{%- endif -%}

<figure class="
  {% if image.aspectRatio | length %}
    grid-image--aspect{{ image.aspectRatio }}
  {% else %}
    grid-image
  {% endif %}
">
  <a href="{{ image.getUrl() }}" data-fancybox="gallery">
    <img
      class="lazyload"
      src="{{ image.getUrl(small) }}"
      data-src="{{ image.getUrl(medium) }}"
      data-sizes="auto"
      data-srcset="{{ imageFunctions.srcset(image, outputWidths, aspectRatio) }}"
      {% if image.alt %}alt="{{ image.alt }}"{% endif %}>
  </a>
  {% if image.caption %}
  <figcaption>{{ image.caption }}</figcaption>
  {% endif %}
</figure>
