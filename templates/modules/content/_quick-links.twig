{%- set nav = entry.quickLinks -%}
<ul>
  {% nav item in nav %}
    {% if item.type == "external" %}
      <li class="quick-links--item"><a href="{{item.linkUrl}}"{% if item.newWindow %} target="_blank"{% endif %}>{{item.linkTitle}}</a></li>
    {% elseif item.type == "internal" %}
      {%- set entry = item.entry.one() -%}
      <li class="quick-links--item"><a href="{{entry.url}}" title="{{ entry.title }}">{{ entry.title }}</a></li>
    {% endif %}
  {% endnav %}
</ul>
